#include<bits/stdc++.h>
#define forn(i, n) for (int i = 0; i < int(n); i++)
using namespace std;

int main(){
ios_base::sync_with_stdio(false);
cin.tie(NULL);
int q;
while(q--){
    string k;cin>>k;
    int n = k.size();
    int r = stoi(k);

    cout<<r;
}

// string s;cin>>s;
// cout<<s.size();
return 0;
}